# Database Configuration (required)
DATABASE_URL=postgresql://username:password@hostname:port/database_name?sslmode=require

# Email Configuration (required for sending confirmation emails)
GMAIL_USER=your-gmail-account@gmail.com
GMAIL_APP_PASSWORD=your-16-character-app-password

# Important: For Gmail accounts with 2FA enabled, you must use an App Password
# To generate an App Password:
# 1. Go to your Google Account settings (https://myaccount.google.com)
# 2. Navigate to Security > App passwords (requires 2FA to be enabled)
# 3. Select "Other (Custom name)" from the dropdown
# 4. Name it "Kayo Website" or something recognizable
# 5. Click "Generate" and copy the 16-character password
# 6. Paste it above as GMAIL_APP_PASSWORD (no spaces needed)

# Email sender details
EMAIL_FROM_NAME=Kayo Platform
EMAIL_FROM_ADDRESS=your-gmail-account@gmail.com  # Should match GMAIL_USER

# Admin notifications recipient (where form notifications will be sent)
ADMIN_EMAIL=your-admin-email@example.com

# Application environment
NODE_ENV=development  # Change to "production" for production deployment

# Database initialization flag
# Set to true to initialize the database tables on startup
# Only needed when running the application locally for the first time
# For production, use the scripts/db-init.js script instead
INITIALIZE_DB=false